# Configuration SPÉCIFIQUE service-client
spring:
  application:
    name: service-client

server:
  port: 8082

# Base de données service-client
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/client_db?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: Kawtar

logging:
  level:
    com.exemple.client: DEBUG