spring:
  application:
    name: service-produit

server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/produit_db?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: Kawtar